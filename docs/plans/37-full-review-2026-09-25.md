# 37. 요구사항 기준 전체 검토 — 정보 구성 · 모바일 탭 UX · 고도화 (2026-09-25)

## 요청

> 모바일에서 전체 자산 모니터링 → 투자 목표(자산·배당) 설정 → 단기/중기/장기 × ISA/연금저축/IRP/일반/해외
> 계좌특성별 비중 추천 → 계좌별 절세 계획 → 주기적 시장 모니터링 + 비중 이탈 신호로 리밸런싱 징후 분석 →
> 매수/매도 알림 → 수동/자동 매매로 리밸런싱. 현재 기능·정보를 요구사항 기준으로 전체 검토하고, 탭별 모바일
> UI/UX를 파악해 불필요한 것은 제거·병합하고, 고도화 항목을 리스트업·계획.

2026-08-31(계획서 `purrfect-greeting-cat`, 메모리 `project_ux_dedup_2026-08-31`)에 같은 취지 감사를 한 번
했으므로 그 결론을 코드와 재대조하고, 그 뒤 추가된 기능(토스 연동·챌린지·DCA 자동매수·ISA 절세액·AUTO 한도
UI 등)을 포함해 탭별 인벤토리를 새로 떴다(Explore 3개 병렬: 홈·자산 / 리밸런싱·계획·설정 / 백엔드 요구사항
커버리지).

기준 커밋: `9aabe43` (main). 작업 브랜치: `fix/full-review-2026-09-25`.

---

## Part 1 — 요구사항 대비 현황

| 요구사항 | 상태 | 핵심 위치 | 갭 |
|---|---|---|---|
| A. 전체 자산 모니터링 | ⚠️→✅ (이번 세션 수정) | `portfolio_service.build_portfolio_overview`, `composition_calculator`, `asset_aggregator` | **토스 계좌가 주식 집계에서 누락되던 실버그** — 아래 P0-1. 부채(대출)는 주담대만, 코인·퇴직연금(DB/DC) 미모델 |
| B. 투자 목표 (자산·배당) | ✅ / 부분 | `UserSettings.goal_*`, `GoalSettingWizard`, `jobs/goal_achievement` | 기간별(단기/중기/장기) **목표 금액**은 없음 — horizon은 계좌 태그일 뿐, by-horizon 추천은 목표 역산 안 함 |
| C. 기간 × 계좌특성별 비중 추천 | 부분 | `goal_horizon_recommendation_service`, `goal_candidate_service._TAX_TYPE_MARKET_GROUP`, IRP 안전자산 30% | 계좌특성은 **그룹별 시장 필터**로만 사용. ① 계좌 간 **자산 배치(asset location)** 최적화 없음(그룹마다 독립 최적화) ② 연금저축·IRP에 레버리지/인버스 ETF 배제·개별주 금지 규칙 없음(보유 종목이 후보로 시드됨) ③ ISA 편입 가능 상품 제한 없음 |
| D. 계좌별 절세 계획 | 부분(표시 위주) | `tax_service`, `isa_service`, `pension_contribution_service`, `TaxTabContainer` | 연금/IRP 세액공제 **금액**(16.5/13.2%) 미계산, 월별 납입 스케줄 없음, ISA 만기→연금 이전(추가 300만 공제) 안내 없음, ISA 연 2천만 납입한도 미추적, 해외 250만 공제 활용 이익실현(gain harvesting) 제안 없음, 손실수확은 해외만·주문 미연결, 해외 실현손익은 수기 입력. **금융소득 2천만 판정 오류**(P0-2, 수정) |
| E. 주기적 시장 모니터링 | ✅ | `market_signal_service`(FRED 9계열 복합점수 + hysteresis), 시간별 알림·08:30 다이제스트 | 국내 지표는 환율뿐, 임계값 백테스트 근거 없음(기존 로드맵 C3~C5) |
| F. 드리프트→신호→알림→수동/자동 실행 | ✅ (KIS·키움) | `analyze_rebalancing`, `alert_check`, `_dispatch`(이메일+FCM), 2단계 플랜(매수 지연자동/매도 이메일승인), 게이트 3종+건당/일일 한도, DCA 프리셋 | 토스는 주문 API 없어 조회 전용. DCA는 계좌 내 예수금으로만 매수(예수금 부족 사전 경고 없음) |
| G. 추천 → 포트폴리오 반영 | ✅ (클라이언트) | `RecommendationApplySection` → `PUT /portfolios/{id}` + 비교 미리보기 | 8/31 R1은 이미 충족. 단 "기준 포트폴리오"가 지정돼 있어야 적용 버튼이 뜸(아니면 안내문만) — UX 갭 U5 |

**종합**: 파이프라인은 끝까지 연결돼 있다. 남은 갭은 (1) 이번에 찾은 **집계/세금 정확성 버그 2건**, (2) 요구사항
"계좌 특성을 고려한 비중 추천"의 깊이(자산 배치·연금 규정), (3) "절세 계획을 세워주고"의 능동성(금액·시점
제안), (4) 탭 간 정보 중복과 모바일 화면 밀도.

---

## Part 2 — 이번 세션 구현 완료

### P0-1. 토스 계좌가 주식 집계 7곳에서 누락 (실버그)
토스 연동(`b8a3d37`) 때 `STOCK_TOSS` enum만 추가되고 서비스별 로컬 주식 타입 집합에는 안 들어갔다. 결과:
토스 계좌는 스냅샷 총액만 합산되고 **주식평가액·매입원가·손익·종목 상세·종목 비중에서 빠짐**, 예수금 분리 안 됨,
대시보드 비주식 기준선에 주식이 섞여 수익률 왜곡, 세금 추정·자산추이 주식 분해·추천 드리프트 알림·백테스트
실보유 비교에서도 제외.
- `app/constants.py`에 `POSITION_STOCK_ASSET_TYPES` 단일 정의, `portfolio_service`(STOCK_TYPES/_EQUITY_TYPES),
  `composition_calculator`, `tax_service`, `portfolio_history_service`(집합+raw SQL), `recommendation_drift_alert_service`,
  `backtest_service`, `asset_aggregator`(raw SQL)가 참조하도록 통일. 라벨 누락(키움/토스) 보강.
- 재발 방지: `tests/test_constants.py` — `AssetType`의 모든 `STOCK_*`가 집합에 있어야 통과(다음 브로커 추가 시
  CI가 잡음). `test_build_portfolio_overview`에 토스 파라미터 추가.

### P0-2. 금융소득 종합과세(2천만) 판정 오류
- `tax_service.get_tax_summary`가 **해외주식 미실현 양도차익을 금융소득에 합산** → 해외 이익이 큰 사용자에게 종합과세
  경고가 잘못 뜨고 잔여 한도가 과소 표시. 해외 양도차익은 양도소득 분류과세라 합산 대상 아님 → 배당만으로 판정.
- `_calc_dividend_income`이 **ISA·연금저축·IRP 계좌 배당까지 15.4% 과세·금융소득에 포함** → 세제혜택 계좌 제외
  (계좌 미지정 배당은 과세로 간주). 건강보험 피부양자 추정도 같은 값을 쓰므로 함께 교정.
- 프론트 문구 3곳("금융소득(배당+해외차익)", 플래너 안내) 정정, 배당소득세 보조문구 "과세계좌 배당".

### UX 저위험 수정
- `AutoInvestStatusBanner`(계획탭): "설정 관리/설정하기"가 목록만 열고 알림 모달은 안 열던 죽은 딥링크 →
  대상이 하나로 정해지면 `portfolioId&openAlert=1`.
- `GoalSettingWizard` 6단계 "이 추천으로 포트폴리오 만들기": 생성 후 계획탭에 머물러 계좌 미연결 고아
  포트폴리오가 남던 흐름 → 생성된 포트폴리오로 이동 + "계좌를 연결하면 진단 시작" 안내.
- 설정탭 "계좌 연동": 전역 KIS 키(`has_kis`)만 봐서 키움·토스 사용자는 "미연결"로 표시 → API 연동 계좌 수 표시,
  라벨 "KIS/키움/토스".
- 계획탭 안내문이 실제 없는 버튼명("처음부터 다시 설정/값만 빠르게 수정")을 가리키던 것 정정, 통계 라벨의
  메타정보 "(대시보드 연동)" 제거, "대시보드" → 네비 명칭 "홈 탭"으로 통일.
- 자산탭 중복 크롬 제거: 탭 바로 아래 "자산 › 투자현황/계좌관리" 브레드크럼 2개 + 계좌관리 소개문.
- 빈 상태 조사 오류 "등록된 은행계좌이 없습니다" → "가".
- stale 주석 2곳(`DashboardPage` 스냅샷 카드, `TaxLimitsBanner`) 현행화.

---

## Part 3 — 탭별 모바일 UX 개선 계획 (미착수)

원칙: 지표마다 권위 표면 1곳 + 나머지는 압축 포인터. 홈 = "지금 할 일", 상세는 소유 탭. 5탭 구조 유지.

### 홈 (`DashboardPage`) — 현재 기성 사용자 6카드 ≈ 1,350~1,450px(폰 2화면)
| ID | 내용 | 파일 | 리스크 |
|---|---|---|---|
| U1 | 같은 값 이중 표기 제거: Hero 총자산(축약+원단위), `InvestmentSnapshotCard` 3칸 × (축약+원단위) = 9줄 → 축약 1줄(원단위는 `title`/탭 시 토글). Hero 환율 슬롯은 "이번 달 변동"(전월 말 대비)로 교체 검토 | `HeroSummaryCard`, `InvestmentSnapshotCard` | 낮음 |
| U2 | `RebalancingStatusCard` 기본 접힘 조건화: 리밸런싱 필요 포트폴리오가 있을 때만 펼침, 아니면 접힘+"모두 정상" 힌트 | `RebalancingStatusCard`(홈 인스턴스 prop) | 낮음 |
| U3 | 주식 평가액·손익이 4곳(홈 스냅샷/자산 투자현황 요약/증권계좌 요약/보유종목 헤더) — 보유종목 테이블 헤더 합계 제거, 증권계좌 요약은 계좌관리 맥락이라 유지 | `StockHoldingsTable` | 낮음 |

### 자산 (`AssetsPage`) — 최대 4단 탭(네비→투자현황/계좌관리→종목/배당/세금→하위)
| ID | 내용 | 파일 | 리스크 |
|---|---|---|---|
| U4 | `StockAccountCard` 5개 수치(요약카드와 동일 5종 반복) → 평가금액·손익만, 나머지는 펼침 | `StockAccountCard` | 낮음 |
| U5a | 입출금·배당 필터 3 드롭다운 한 줄 → 계좌 드롭다운 + 유형 칩 | `TransactionHistoryTab` | 낮음 |
| U5b | 모바일 보유종목 정렬(현재 데스크톱 헤더만) — 07-20 "정렬 토글" 구현분이 모바일 카드에 연결됐는지 재확인 후 보완 | `StockHoldingsTable` | 낮음 |

### 리밸런싱 (`RebalancingPage`) — 포트폴리오 서브탭이 추천/목록/분석·실행/백테스트 4역할
| ID | 내용 | 파일 | 리스크 |
|---|---|---|---|
| U6 | **추천을 독립 서브탭으로**: 진단 / 추천 / 포트폴리오 / 이력. `RecommendationCard`(항상 펼침, 사용자 포트폴리오 목록 위를 점유)를 분리 → 포트폴리오 탭은 목록+분석·실행 전용 | `RebalancingPage`, `constants/tabs` | 중(딥링크 `rtab=포트폴리오` 사용처 갱신 필요 — 설정/계획/홈 링크) |
| U7 | 추천 적용 시 "기준 포트폴리오" 미지정이면 버튼 대신 안내문만 → 적용 대상 포트폴리오 선택 드롭다운(기준 우선 정렬) | `RecommendationApplySection` | 낮음~중 |
| U8 | 알림 모달 호스트 2개(`PortfolioManageTab`/`PortfolioExecutionTab`) + `AutomationStatusBar` 최대 3회 → 페이지 단일 호스트, 선택 포트폴리오는 분석 패널에서만 1회 | `PortfolioManageTab`, `PortfolioExecutionTab`, `AnalysisPanel` | 중 |
| U9 | 진단탭 매크로 카드 2개(`MarketSignalBanner` + `InflationSummaryCard`) → 물가를 시장신호 배너의 매크로 지표 확장 영역으로 병합, GREEN이면 접힘 | `MarketSignalBanner`, `InflationSummaryCard` | 낮음 |
| U10 | 용어 통일: "리밸런싱 알림/자동화 설정/알림 버튼" → "자동화 설정", 진단탭 첫 카드 제목 "투자 현황 진단" → "포트폴리오 이탈 현황"(서브탭명과 중복 해소) | 다수(문자열) | 낮음 |

### 계획 (`InvestPlanPage`) — 2단 pill 행
| ID | 내용 | 파일 | 리스크 |
|---|---|---|---|
| U11 | `GOAL_TABS`(목표 현황/저축 시뮬레이터) 제거 → 시뮬레이터를 적립 계획 최하단 접힘 카드로 | `InvestPlanPage` | 낮음 |
| U12 | 투자성향이 마법사("투자성향")·추천옵션 모달("리스크 성향") 두 이름 → "투자성향"으로 통일, 연령(출생연도 vs 연령대) 입력 2벌 → 옵션 모달은 출생연도 표시만 | `GoalRecommendationOptionsModal` | 낮음 |

### 설정 (`SettingsPage`, `/settings/notifications`)
| ID | 내용 | 파일 | 리스크 |
|---|---|---|---|
| U13 | **AUTO 하루 거래한도는 알림이 아니라 매매 안전장치** → 알림 모달 AUTO 섹션(이미 읽기전용 표시 중)에서 직접 편집, 알림설정 페이지에선 제거 | `AutoRebalancingDailyCapSection`, `AlertAutoModeSection` | 중 |
| U14 | 알림설정 SectionCard 중첩(외곽 안에 3개) 평탄화, 요약 카운트에 시장신호 알림 포함 | `NotificationSettingsPage`, `SettingsPage` | 낮음 |
| U15 | DART 키 카드(개발자성 설정) 최상단 → "고급" 섹션 하단. "백테스트 기능 표시" 토글 제거하고 포트폴리오탭 하단에 접힌 백테스트 카드 상시 노출 | `SettingsPage`, `RebalancingPage` | 낮음 |
| U16 | 딥링크 완결: 설정 "목표 역산 추천 옵션" → 옵션 모달 자동 오픈(`?openRecOptions=1`), 추천카드 "목표 설정하러 가기" → 저장 후 복귀 경로 | `SettingsPage`, `RecommendationCard` | 낮음 |

**권장 순서**: U1·U2·U9·U11·U14·U15(저위험 묶음) → U4·U5 → U7·U10·U12·U16 → U6·U8·U13(구조 변경, 각각 단독 PR).

---

## Part 4 — 고도화 로드맵

| # | 항목 | 요구사항 연결 | 규모 | 리스크 | 메모 |
|---|---|---|---|---|---|
| E1 | **연금저축·IRP 규정 준수 필터**: 레버리지/인버스 ETF·개별주 후보 배제(보유종목 시드 포함), 연금저축도 위험자산 한도 안내 | C | 소~중(백) | 낮음 | 추천이 법적으로 매수 불가한 종목을 내놓을 수 있는 정확성 문제 → **최우선** |
| E2 | **절세 액션 플랜**(구 R2): 연금/IRP 세액공제 금액(총급여 5,500만 기준 16.5/13.2%) + 연말까지 남은 권장 납입액·월 분할, ISA 만기 60일 내 연금 이전(+300만 공제) 안내, ISA 연 2천만 납입 한도 추적, 해외 250만 공제 활용 이익실현 제안, 손실수확 국내 확장 → 액션 리스트 1카드 | D | 중~대(백+프) | 중 | 소득구간 입력 1필드 필요. 연말 리마인더 잡과 연결 |
| E3 | **계좌 간 자산 배치(asset location)**: 전체 목표 비중을 정한 뒤 채권·고배당·해외지수ETF는 ISA/연금으로, 성장·국내주식은 일반으로 배치하는 2단계 최적화 | C | 대(백) | 중 | by-horizon 그룹 독립 최적화의 상위 레이어. E1 선행 |
| E4 | 기간별 목표 금액(단기/중기/장기) + by-horizon 목표 역산 | B·C | 중 | 중 | 스키마 추가(`horizon_goals` JSONB) |
| E5 | DCA 예수금 부족 사전 알림(실행 D-2, 필요액 vs 주문가능액) + 입금 확인 후 재시도 | F | 소~중 | 낮음 | `orderable_krw`(D+2) 재사용 |
| E6 | 해외 실현손익 자동 집계(브로커 체결내역) → 250만 공제 잔여 정확화, 수기 입력 대체 | D | 중 | 중 | KIS/키움 체결조회 API 조사 |
| E7 | 이자소득 추적(예금·CMA) → 금융소득 2천만 판정 완성 | D | 소 | 낮음 | P0-2 후속 |
| E8 | 시장신호 국내 지표·점수 구간 게이트·임계값 백테스트 (구 C3~C5) | E | 대(리서치) | 높음 | 계획 21 Phase 2/3 |
| E9 | ETF 총보수(TER) 표시·추천 반영 (구 C2) | C | 중 | 중 | 데이터 소스 조사 선행 |
| E10 | 토스 주문 실행 — 토스 Open API 주문 엔드포인트 공개 시 | F | 중 | 높음 | 모니터링 |

**권장 착수**: E1 → E5 → E2 → E7 → E3/E4.

---

## 검증

- 백엔드: `pytest` 전체 2207 passed(신규 `test_constants.py`, 토스 파라미터, 금융소득·배당 제외 테스트), ruff/format/mypy clean.
- 프론트: vitest 전체 1560 passed(112 files), `tsc --noEmit`, eslint(max-warnings 0), prettier 통과. 변경 테스트: `components.autoInvestStatusBanner`, `components.goalSettingWizard`,
  `pages.settings`, `pages.low-coverage`.
- 배포 후 확인: 토스 계좌 보유 사용자의 홈 "주식 투자 현황" 평가액·손익, 자산구성 도넛에 토스 주식/예수금 분리 반영.
  대시보드 캐시(TTL)가 만료되거나 동기화 후 갱신됨.
